## What this changes

<!-- One or two sentences. What can a user do now that they could not before? -->

Closes #

## How I tested it

<!-- Not "it works". What did you actually run or click?
     e.g. "Seeded, logged in as student01, opened /attendance, saw 78% for DBMS
     which matches the 7/9 sessions in the seed data." -->

## Checklist

- [ ] I ran `npm run lint`, `npm run typecheck` and `npm run test` locally
- [ ] I added tests for the happy path, the auth requirement, and one abuse case
- [ ] I only touched files my team owns (see `.github/CODEOWNERS`)
- [ ] If I added an API endpoint, its schema is in `packages/validation`
- [ ] If I added a screen, it renders loading, empty and error states
- [ ] I did not add a dependency (or I opened a dependency request issue first)
- [ ] This PR is under ~400 lines — if not, I explained why below

## Screenshots

<!-- Frontend changes: before and after. Delete this section for backend PRs. -->
